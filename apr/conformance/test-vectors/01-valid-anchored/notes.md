# 01-valid-anchored

Baseline. Single-claim day. Signature verifies under `test-claim-2026`, claim id is the only leaf in the Merkle root, root signature verifies under `test-root-2026`. Conforming verifier MUST return `verified`.
